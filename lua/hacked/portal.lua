local M = {}

M.open = function ()
    -- open borderless float 
    -- pass in filename or optionally a buffer
    -- enter automatically
    -- use extmark below this "portal" to push out the code?
    -- or we can shove the portal into the corner
end

return M
